extends layout
block post
    .container.post
        h1 Posts 
        each post in posts 
            div 
                h2= post.name 
                img(src=post.image alt="Image du post")
                p= post.legend 
                span Likes : #{post.likes}

